export function ButtonStyle({
  backgroundColor,
  buttonColor,
  borderRadius,
  buttonPadding
}) {
  return (
    <div>
      <h1>Hello CodeSandbox</h1>
      <button
        style={{ backgroundColor, buttonColor, borderRadius, buttonPadding }}
      >
        Start
      </button>
    </div>
  );
}
